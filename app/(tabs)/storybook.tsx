import React from "react";
import SimpleStorybook from "../../packages/task-system/.ondevice/SimpleStorybook";

/**
 * Storybook screen for browsing and testing UI components
 *
 * Displays component stories from the @orion/task-system package.
 * Browse categories and tap any story to view and interact with it.
 */
export default function StorybookScreen() {
  return <SimpleStorybook />;
}
