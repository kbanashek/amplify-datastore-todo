/**
 * Package source identifier constant for logging
 * Use this constant instead of string literals when identifying the package source in logs
 */
export const PACKAGE_SOURCE = "task-system" as const;
